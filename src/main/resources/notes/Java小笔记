java8特性
    1.lambda表达式
    2.stream
    3.接口默认实现
    4.::关键字
    
1.lambda表达式
    可以用（参数列表）->{
        实现
        return 返回值
    }
    作为参数。
2.Iterator的一种操作方式，可以filter、map、collect、count等

3.可以用default关键字，给接口方法一个默认实现

4.::关键字，相当于将原有的方法抽取成lambda表达式，或者作为“函数式”对象存在，用于调用方法
